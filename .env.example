# SQLite local (rápido para hoy)
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="please-change-me-32-chars-min"
NEXTAUTH_URL="http://localhost:3000"

# If deploying to Vercel, set NEXTAUTH_URL to your domain
# and switch DATABASE_URL to a hosted Postgres (e.g. Neon/Supabase):
# DATABASE_URL="postgresql://user:password@host:port/db"
